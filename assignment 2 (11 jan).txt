Q1.create a new branch in your git repo names as assignment2(paste the command).

Solution => Steps to create new branch :- 
1. Cd “ goto current directory” 
2. Type command (git branch) and check in which branch you reside right now.
3. Command to create assignment2 branch :- git checkout -b assignment2

 

Q2.Tell the difference between nodejs and javascript.
  
Solution => Following are the difference between nodejs and javascript:-
1. NodeJS is a Javascript runtime environment.whereas,Javascript is a programming language
   that is used for writing scripts on the website. 
2. NodeJS is mostly used on the server-side.whereas,JS is basically used on the client-side.
3. Nodejs does not have capability to add HTML tags.whereas,Javascript is capable enough 
   to add HTML and play with the DOM. 
4. Nodejs is used in server-side development.whereas,Javascript is used in frontend development.
5. We can run Javascript outside the browser with the help of NodeJS.whereas,Javascript can only
   be run in the browsers.



Q3.Install a package using npm use it(show screen shot of package.json).
Solution => Command for installing a package using npm :- npm init


Q4. git command to reset a commit.
Solution => git command to reset a commit:- git reset commit-id



Q5. write a simple program in javascript and run it using nodejs.
Solution =>
   var a=23;
   var b=45;
   console.warn(a+b);

output:-
PS C:\Users\Hp\Desktop\MY REPO\Hitbullseye> node index.js
68

